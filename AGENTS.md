# Agent entrypoint

All agents must begin with `PROJECT.md`, then `STATUS.md`, `DECISIONS.md` and `INDEX.md`.

This is an AI-oriented project brain. Keep documentation concise, canonical and portable.

Legacy is read-only by default. V2 is the writable project.

Do not duplicate the full project rules here.
